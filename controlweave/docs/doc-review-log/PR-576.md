# Documentation review — PR #576

Performed live by the Claude Code session subscribed to this PR, per
`.claude/rules/doc-review.md`, triggered by `docs-pipeline.yml`'s
"Documentation Update Queued" comment (flagged `frameworks`, affecting
`docs/guides/FRAMEWORKS.md`, `docs/guides/GETTING_STARTED.md`,
`docs/USER_GUIDE.md`; the automated generator itself produced no diff).

Scope: the three flagged docs plus `docs/frameworks/NIST_800_53.md`
(found via `docs/USER_GUIDE.md`'s own link, not flagged directly by the
pipeline but clearly the primary doc this PR's change affects), reviewed
against the actual diff (`nist_800_53` seeded control count 154 → 300,
generated from the official NIST OSCAL catalog instead of hand-typed).

## Fixed (real, verified discrepancies)

- **`docs/frameworks/NIST_800_53.md`** — the summary line claimed
  ControlWeave implements "1,000+ controls and control enhancements."
  This conflated the *official standard's* total scope (base controls +
  enhancements, ~1,170 non-withdrawn items combined) with what
  ControlWeave actually seeds (this PR's 300 base controls; control
  enhancements are explicitly not seeded, by this PR's own stated
  scope). Rewrote to state the real seeded count and explicitly note
  enhancements aren't yet included, rather than let a true-sounding
  "1,000+" imply enhancement coverage that doesn't exist.
- **Same file's per-family control-count table** — cross-checked all 20
  rows against the actual OSCAL catalog (excluding withdrawn controls,
  matching what's actually seeded). Two rows were already wrong before
  this PR (MA: doc said 6, actual is 7; SA: doc said 23, actual is 17 —
  the old 23 appears to have counted withdrawn SA controls, of which
  there are 7). Corrected all 20 rows to verified figures; they now sum
  to exactly 300, matching the seeded total.

## Verified already correct — no change needed

- **`docs/guides/FRAMEWORKS.md`** line 14: "NIST 800-53 - Comprehensive
  federal security controls (20 families, 300+ controls)" — this line
  predates this PR and was *previously false* (only 154 controls were
  actually seeded, 13 of 20 families). This PR makes it true for the
  first time rather than requiring an edit. Worth noting explicitly per
  the doc-review rule's point about confirming what's accurate, not just
  reporting problems — this is the inverse case, an existing claim that
  silently became accurate.
- **`docs/guides/FRAMEWORKS.md`** (remaining NIST 800-53 mentions),
  **`docs/guides/GETTING_STARTED.md`** — spot-checked all NIST 800-53
  references (AC-1/AC-2 examples, "comprehensive security controls"
  labels); none make a specific count claim this PR's change affects.
- **`docs/USER_GUIDE.md`** line 146 — only links to
  `frameworks/NIST_800_53.md`, no inline duplicated content; the linked
  page is the one actually fixed above.

## Noted, not fixed (out of scope for this PR)

- None found for this diff.
