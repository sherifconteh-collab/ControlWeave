# Documentation review — PR #616

Performed live by a Claude Code session with direct PR/repo access (no
GitHub Action, no separate API key), per `.claude/rules/doc-review.md`.

Scope: `docs-pipeline.yml`'s automated feature-change detector flagged
this PR as an `authentication` UI change affecting
`docs/guides/GETTING_STARTED.md` and `docs/guides/ACCOUNT_SETUP.md`.

## Verified already correct — no change needed

- **`docs/guides/GETTING_STARTED.md`** — searched for tier/pricing/trial
  language; the only related content is "ControlWeaver has no tier-based
  AI request limit" (line 331), which is already consistent with this
  PR's removal of the registration page's fictitious tier gating. Nothing
  in this file described the removed Community/Pro/Enterprise/Gov Cloud
  picker, pricing text, or per-tier framework limits, so nothing needed
  fixing.
- **`docs/guides/ACCOUNT_SETUP.md`** — same check. The only tier
  references found are "1000+ employees: Enterprise" (an org-size
  suggestion unrelated to the registration form's removed gating) and
  "(All tiers)"/"(all tiers — recommended)" notes on TOTP 2FA, both of
  which already describe TOTP as available regardless of tier — consistent
  with, not contradicted by, this PR. No edits made.

## Notes

This PR only changes `controlweave/frontend/src/app/register/page.tsx`
(removes a frontend-only tier/pricing picker that had no backend
enforcement to begin with) — it doesn't change any registration
steps, required fields, or account-setup behavior a user-facing guide
would need to describe differently.
