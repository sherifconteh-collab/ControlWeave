# Documentation review — PR #612

Performed live by a Claude Code session with direct PR/repo access (no
GitHub Action, no separate API key), per `.claude/rules/doc-review.md`.

Scope: `docs-pipeline.yml`'s automated feature-change detector flagged this
PR as a `controls` UI change affecting `docs/guides/CONTROLS.md`,
`docs/guides/GETTING_STARTED.md`, and `docs/USER_GUIDE.md`. The generator's
own doc-update-PR step produced no diff, so this review checks those three
files by hand against the actual code diff.

## Fixed (real, undocumented feature)

- **`docs/guides/CONTROLS.md`** — this PR adds a new "AI Control
  Assessments" review page (`dashboard/controls/pending-assessments`,
  reachable from the sidebar under Compliance) that lets connector-derived
  evidence trigger an AI-suggested control status change, staged for human
  approval/rejection (`POST /scan`, `GET /`, `GET /stats`,
  `POST /:id/approve`, `POST /:id/reject`). None of the existing guide
  content mentioned this — added a new "AI Control Assessments
  (Connector-Driven Suggestions)" section (after "Import / Export", before
  "Assessment Procedures") describing the scan/review/approve/reject flow,
  the Admin/Auditor-only rule for approving a suggestion that sets a
  control to "Verified" (mirrors the existing manual status-update RBAC
  rule), and that AI-suggested regressions are allowed here by design
  (unlike the manual workflow's forward-only ordering).

## Verified already correct — no change needed

- **`docs/guides/GETTING_STARTED.md`** — no content describes per-feature
  sidebar pages at this level of detail; nothing in this PR's diff
  contradicts it.
- **`docs/USER_GUIDE.md`** — the Quick Navigation / Learning Paths
  structure links to guide pages, not individual sub-features within a
  guide (Control Exceptions and Crosswalk Mappings, both existing
  CONTROLS.md sub-sections, aren't separately linked here either) — no
  entry needed for consistency with how the rest of the Controls guide's
  sub-features are surfaced.

## Noted, not fixed (out of scope for this PR)

- None found. This PR's diff is additive only (new migration, new route
  file, new frontend page, sidebar entry, API client wrapper) — nothing it
  touches contradicts existing documented behavior elsewhere.
