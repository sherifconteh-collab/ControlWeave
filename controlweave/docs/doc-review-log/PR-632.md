# Documentation review — PR #632

Performed live by a Claude Code session with direct PR/repo access (no
GitHub Action, no separate API key).

Scope: no docs-pipeline queued-comment fired for this PR; the cue was the
diff itself — it changes CI behavior (`security-pipeline.yml` `dast-scan`
job, `release-notes.yml` / `docs-pipeline.yml` push steps, `.zap/rules.tsv`)
that `CI_CD_GUIDE.md` and `SECURITY_SUMMARY.md` describe, plus a
`controlweave/marketing` lockfile security patch.

## Fixed (real, verified discrepancies)

- **`CI_CD_GUIDE.md` §1.2** — the `dast-scan` row described the job as an
  unconditional "OWASP ZAP baseline scan against
  `https://demo.controlweave.com`". As of this PR the job first probes the
  target and skips the scan (with a workflow notice) when the demo
  environment is unreachable; the row now says so.

## Verified already correct — no change needed

- **`CI_CD_GUIDE.md` §1.2** — the claim that `dast-scan` is "Not in the
  gate at all" and "only runs on push to `main`, never on PRs" remains
  accurate; this PR does not change the job's trigger conditions or its
  exclusion from the `✅ All Security & QA Checks Passed` gate.
- **`SECURITY_SUMMARY.md`** — the DAST references (§ security testing,
  APSC-DV-001630) describe *what* is scanned and defer to
  `CI_CD_GUIDE.md` §1 for blocking status; still accurate after this PR.
- **`CI_CD_GUIDE.md` §1 NIST table (SA-11, RA-5(3))** — the DAST coverage
  claims remain true: the scan still runs whenever the demo target is up;
  this PR only stops an offline demo environment from failing the
  pipeline.
- The release-notes/docs-pipeline push-retry change is invisible to users
  and operators (same commits, same messages); no doc page describes the
  push mechanics.

## Noted, not fixed (out of scope for this PR)

- The v4.8.0 version bump for PR #631 was lost to the push race this PR
  fixes: `CHANGELOG.md` / `RELEASE_NOTES.md` / package versions still say
  v4.7.0 even though #631 merged. The release-notes workflow supports
  `workflow_dispatch` and will regenerate on the next merge to `main`, so
  no manual version edits were made here.
