# Ignore moderate ajv vulnerabilities in dev dependencies
# These are not exploitable in the ESLint context as ESLint doesn't use $data: true
audit-level=high
# @sentry/nextjs peer-dep range doesn't yet include next@16; override allows install
legacy-peer-deps=true
